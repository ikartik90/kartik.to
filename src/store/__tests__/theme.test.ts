// Tests for theme store helpers.
// TODO: implement once resolveTheme is exported from the theme store.

import { describe, it } from "vitest";

describe("theme store", () => {
  it.todo("resolveTheme returns light for light preference");
  it.todo("resolveTheme returns dark for dark preference");
});
